[pytest]
# 通常の `pytest` / `docker compose ... run test` では Playwright 依存の UI テストを含めない。
# UI テストは `pytest tests/ui` または `docker compose ... run test-ui` で明示実行する。
testpaths =
    tests/unit
    tests/integration
pythonpath = .
addopts = -v --tb=short
