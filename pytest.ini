[pytest]
# 通常の `pytest` / `docker compose ... run test` では Playwright 依存の UI テストを含めない。
# UI テストは `pytest app/tests/ui` または `docker compose ... run test-ui` で明示実行する。
testpaths =
    app/tests/unit
    app/tests/integration
pythonpath = app
addopts = -v --tb=short
